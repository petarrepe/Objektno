@model IEnumerable<KonobApp.Model.Models.TableModel>

@{
    ViewBag.Title = "Index";
}

<p>
    @Html.ActionLink("Stvori novi podatak", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Caffe.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsOccupied)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Caffe.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsOccupied)
        </td>
        <td>
            @Html.ActionLink("Uredi", "Edit", new { id=item.IDTable }) |
            @Html.ActionLink("Detalji", "Details", new { id=item.IDTable }) |
            @Html.ActionLink("Obriši", "Delete", new { id=item.IDTable })
        </td>
    </tr>
}

</table>
@Html.ActionLink("Povratak na glavni meni", "Index", "StartingPage")
